// This file is kept for compatibility but content is no longer seeded to the backend.
// All content is served from local data files (chapters.ts, interviewRoles.ts).
// The backend is now auth/approval-only.
export const SEED_DATA = null;
